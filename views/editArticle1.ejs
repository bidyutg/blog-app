<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>editting page</h1>

    <!-- <form action="/article/edit/<%= article._id %>" method="POST" >
        <input type="text" name="title" value="<%= article.title %>" placeholder="Title" required>
        <textarea name="content" placeholder="Content" required><%= article.content %></textarea>
        <div class="form-group">
            <label for="image">Image (Optional)</label>
            <input type="file" id="image" name="image" accept="image/jpeg, image/png, image/gif">
        </div>
        <button type="submit">Save</button>
    </form> -->




    <form action="/article/edit/<%= article._id %>" method="POST" enctype="multipart/form-data">
        <div>
        <input type="text" name="title" placeholder="Title" >
       </div>

       
       <div>
        <textarea name="content" placeholder="Content" ></textarea>
    </div>
        <input type="file" name="image" accept="image/*">

    <div>
        <button type="submit">Submit Article</button>
    </div>
    </form>
    
</body>
</html>